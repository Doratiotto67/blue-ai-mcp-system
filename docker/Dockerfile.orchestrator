# Dockerfile.orchestrator
FROM blue-ai-base:latest

WORKDIR /app

# Apenas código específico do agente
COPY agents/orchestrator/server.py .
COPY agents/common/llm_router.py .

ENV PORT=8080
EXPOSE 8080

CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8080"]